[package]
name = "slopos-http-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "HTTP/1.1 client core: URLs, redirects, response heads and chunked bodies, host-testable"

[lints]
workspace = true

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]
doctest = false
