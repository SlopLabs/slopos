[package]
name = "slopos-fat-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "FAT32 over a byte-addressed device: format, read, and copy-on-write replacement of files, host-testable"

[lints]
workspace = true

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]
doctest = false
