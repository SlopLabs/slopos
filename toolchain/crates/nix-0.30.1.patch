diff --git a/build.rs b/build.rs
index eb27943..b23efb7 100644
--- a/build.rs
+++ b/build.rs
@@ -32,4 +32,6 @@ fn main() {
     println!("cargo:rustc-check-cfg=cfg(qemu)");
     // Cygwin target, added in 1.86
     println!("cargo:rustc-check-cfg=cfg(target_os, values(\"cygwin\"))");
+    // SlopOS target, not yet in rustc's built-in list
+    println!("cargo:rustc-check-cfg=cfg(target_os, values(\"slopos\"))");
 }
diff --git a/src/errno.rs b/src/errno.rs
index 497e33d..50058f6 100644
--- a/src/errno.rs
+++ b/src/errno.rs
@@ -33,7 +33,8 @@ cfg_if! {
                         target_os = "dragonfly",
                         target_os = "fuchsia",
                         target_os = "hurd",
-                        target_os = "emscripten"))] {
+                        target_os = "emscripten",
+                        target_os = "slopos"))] {
         unsafe fn errno_location() -> *mut c_int {
             unsafe { libc::__errno_location() }
         }
@@ -276,6 +277,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -286,6 +288,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -296,6 +299,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -306,6 +310,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -316,6 +321,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -326,6 +332,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -336,6 +343,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -346,6 +354,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -356,6 +365,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -365,6 +375,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -374,6 +385,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -383,6 +395,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -392,6 +405,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -401,6 +415,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -410,6 +425,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -419,6 +435,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "hurd",
@@ -429,6 +446,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "hurd",
@@ -439,6 +457,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "hurd",
@@ -449,6 +468,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "hurd",
@@ -459,6 +479,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -468,6 +489,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -477,6 +499,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "hurd",
@@ -487,6 +510,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -496,6 +520,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -505,6 +530,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -514,6 +540,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -523,6 +550,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -533,6 +561,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -542,6 +571,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
             target_os = "cygwin",
@@ -550,6 +580,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "aix",
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -562,6 +593,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "aix",
             target_os = "fuchsia",
             target_os = "haiku",
@@ -573,6 +605,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -582,6 +615,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -591,6 +625,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -600,6 +635,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -609,6 +645,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -618,6 +655,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -627,6 +665,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -636,6 +675,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "hurd",
@@ -646,6 +686,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -657,6 +698,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "aix",
             target_os = "fuchsia",
@@ -666,6 +708,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -675,6 +718,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -684,6 +728,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "netbsd",
             target_os = "redox",
@@ -694,6 +739,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "hurd",
             target_os = "emscripten",
@@ -703,6 +749,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -710,6 +757,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -717,6 +765,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -724,6 +773,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -731,6 +781,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -738,6 +789,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
             target_os = "cygwin",
@@ -746,6 +798,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "openbsd",
             target_os = "dragonfly",
@@ -756,6 +809,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "openbsd",
             target_os = "emscripten",
@@ -764,6 +818,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             solarish,
             target_os = "fuchsia",
             target_os = "haiku",
@@ -774,6 +829,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -781,6 +837,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -788,6 +845,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -795,6 +853,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -802,6 +861,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "aix",
             target_os = "fuchsia",
             target_os = "hurd",
@@ -815,6 +875,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             linux_android,
+            target_os = "slopos",
             target_os = "aix",
             target_os = "fuchsia",
             target_os = "emscripten",
@@ -827,6 +888,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             all(target_os = "linux", not(target_arch = "mips")),
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -834,6 +896,7 @@ fn desc(errno: Errno) -> &'static str {
 
         #[cfg(any(
             all(target_os = "linux", not(target_arch = "mips")),
+            target_os = "slopos",
             target_os = "fuchsia",
             target_os = "emscripten",
         ))]
@@ -1087,7 +1150,12 @@ fn desc(errno: Errno) -> &'static str {
     }
 }
 
-#[cfg(any(linux_android, target_os = "fuchsia", target_os = "emscripten"))]
+#[cfg(any(
+    linux_android,
+    target_os = "fuchsia",
+    target_os = "emscripten",
+    target_os = "slopos"
+))]
 mod consts {
     #[derive(Clone, Copy, Debug, Eq, PartialEq)]
     #[repr(i32)]
diff --git a/src/sys/signal.rs b/src/sys/signal.rs
index 2b0c779..cd081c9 100644
--- a/src/sys/signal.rs
+++ b/src/sys/signal.rs
@@ -61,7 +61,7 @@ libc_enum! {
         /// Software termination signal from kill
         SIGTERM,
         /// Stack fault (obsolete)
-        #[cfg(all(any(linux_android, target_os = "emscripten",
+        #[cfg(all(any(linux_android, target_os = "slopos", target_os = "emscripten",
                       target_os = "fuchsia"),
                   not(any(target_arch = "mips",
                           target_arch = "mips32r6",
@@ -97,18 +97,18 @@ libc_enum! {
         /// Input/output possible signal
         #[cfg(not(target_os = "haiku"))]
         SIGIO,
-        #[cfg(any(linux_android, target_os = "emscripten",
+        #[cfg(any(linux_android, target_os = "slopos", target_os = "emscripten",
                   target_os = "fuchsia", target_os = "aix"))]
         /// Power failure imminent.
         SIGPWR,
         /// Bad system call
         SIGSYS,
-        #[cfg(not(any(linux_android, target_os = "emscripten",
+        #[cfg(not(any(linux_android, target_os = "slopos", target_os = "emscripten",
                       target_os = "fuchsia",
                       target_os = "redox", target_os = "haiku")))]
         /// Emulator trap
         SIGEMT,
-        #[cfg(not(any(linux_android, target_os = "emscripten",
+        #[cfg(not(any(linux_android, target_os = "slopos", target_os = "emscripten",
                       target_os = "fuchsia", target_os = "redox",
                       target_os = "haiku", target_os = "aix",
                       target_os = "solaris", target_os = "cygwin")))]
@@ -141,6 +141,7 @@ impl FromStr for Signal {
             #[cfg(all(
                 any(
                     linux_android,
+                    target_os = "slopos",
                     target_os = "emscripten",
                     target_os = "fuchsia",
                 ),
@@ -170,6 +171,7 @@ impl FromStr for Signal {
             "SIGIO" => Signal::SIGIO,
             #[cfg(any(
                 linux_android,
+                target_os = "slopos",
                 target_os = "emscripten",
                 target_os = "fuchsia",
             ))]
@@ -177,6 +179,7 @@ impl FromStr for Signal {
             "SIGSYS" => Signal::SIGSYS,
             #[cfg(not(any(
                 linux_android,
+                target_os = "slopos",
                 target_os = "emscripten",
                 target_os = "fuchsia",
                 target_os = "redox",
@@ -185,6 +188,7 @@ impl FromStr for Signal {
             "SIGEMT" => Signal::SIGEMT,
             #[cfg(not(any(
                 linux_android,
+                target_os = "slopos",
                 target_os = "emscripten",
                 target_os = "fuchsia",
                 target_os = "redox",
@@ -226,6 +230,7 @@ impl Signal {
             #[cfg(all(
                 any(
                     linux_android,
+                    target_os = "slopos",
                     target_os = "emscripten",
                     target_os = "fuchsia",
                 ),
@@ -255,6 +260,7 @@ impl Signal {
             Signal::SIGIO => "SIGIO",
             #[cfg(any(
                 linux_android,
+                target_os = "slopos",
                 target_os = "emscripten",
                 target_os = "fuchsia",
                 target_os = "aix",
@@ -263,6 +269,7 @@ impl Signal {
             Signal::SIGSYS => "SIGSYS",
             #[cfg(not(any(
                 linux_android,
+                target_os = "slopos",
                 target_os = "emscripten",
                 target_os = "fuchsia",
                 target_os = "redox",
@@ -271,6 +278,7 @@ impl Signal {
             Signal::SIGEMT => "SIGEMT",
             #[cfg(not(any(
                 linux_android,
+                target_os = "slopos",
                 target_os = "emscripten",
                 target_os = "fuchsia",
                 target_os = "redox",
@@ -318,7 +326,7 @@ const SIGNALS: [Signal; 28] = [
     SIGPROF, SIGWINCH, SIGSYS,
 ];
 #[cfg(all(
-    any(linux_android, target_os = "emscripten", target_os = "fuchsia"),
+    any(linux_android, target_os = "slopos", target_os = "emscripten", target_os = "fuchsia"),
     not(any(
         target_arch = "mips",
         target_arch = "mips32r6",
@@ -336,7 +344,7 @@ const SIGNALS: [Signal; 31] = [
     SIGVTALRM, SIGPROF, SIGWINCH, SIGIO, SIGPWR, SIGSYS,
 ];
 #[cfg(all(
-    any(linux_android, target_os = "emscripten", target_os = "fuchsia"),
+    any(linux_android, target_os = "slopos", target_os = "emscripten", target_os = "fuchsia"),
     any(
         target_arch = "mips",
         target_arch = "mips32r6",
@@ -371,6 +379,7 @@ const SIGNALS: [Signal; 30] = [
 ];
 #[cfg(not(any(
     linux_android,
+    target_os = "slopos",
     target_os = "fuchsia",
     target_os = "emscripten",
     target_os = "aix",
@@ -585,7 +594,8 @@ impl SigSet {
 
     /// Suspends execution of the calling thread until one of the signals in the
     /// signal mask becomes pending, and returns the accepted signal.
-    #[cfg(not(target_os = "redox"))] // RedoxFS does not yet support sigwait
+    // RedoxFS does not yet support sigwait, and neither does slibc.
+    #[cfg(not(any(target_os = "redox", target_os = "slopos")))]
     pub fn wait(&self) -> Result<Signal> {
         use std::convert::TryFrom;
 
