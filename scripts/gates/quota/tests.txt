# check_quota_headroom gate data — variant: tests
#
#     scripts/check_quota_headroom.sh --variant tests --emit-allowlist
#
# One line per <phase> <TAB> <kind> <TAB> <cap>, where the cap is the
# highest peak any single account row reached for that kind. Measured,
# never chosen: raise a cap only with a fresh --emit-allowlist in the
# same commit, and say in the message what started consuming more.
#
# A cap matching nothing is a dead entry and fails, because a kind that
# stopped being reported looks exactly like a kind that stayed cheap.
#
# Measured on the tests ISO under quota=enforce, with the session-smoke utest
# holding a desktop-shaped population open across the post-userland-tests
# dump. All eight kinds are charged.
#
# `pages` is mapped **virtual** address space, not resident memory -- the
# RLIMIT_AS quantity. The root's 612 416 pages is 2.3 GiB of VA across ~108
# live address spaces against 86 899 physical pages of RAM, and those two
# numbers are not comparable *by design*: demand paging means a process may
# map far more than it ever populates. The reconciling number is the per-row
# one -- worst single address space 30 998 pages (121 MiB) -- which is what
# the enforced per-process ceiling was derived from. That ceiling is now
# 1 048 576 (4 GiB of VA) and is no longer derived from a measurement at all:
# with lazy mappings a VA charge bounds no memory, so `Pages` is a stated
# `RLIMIT_AS` and `residentpages` below is the number that tracks what is
# actually held.
#
# The caps are the ROOT account's peaks — the sum over every principal plus
# the kernel's own, not a per-process figure. Worst single user rows measured
# 18 fdslot / 10 objectrow / 4 process / 1 task / 12 kernelmeta, against
# enforced per-process defaults of 256 / 512 / 64 / 512 / 2048. Both numbers
# matter and they live in different places on purpose: this file is the gate
# ceiling, the enforced runtime defaults are in abi/src/quota.rs. Deriving
# either from the other would make the ratchet measure its own configuration.
#
# kernelmeta is two things summed on the root. Task stacks are 12 pages each
# — a 32 KiB kernel stack plus a 16 KiB SafeStack data stack — and are the
# deterministic half; the kernel heap's own backing is the other, every page
# the size-class slabs and the large-alloc tier take from the buddy, charged
# once at the allocator so the root's row stays reconcilable against the
# buddy's allocated count. The `memory` kconsole command prints both numbers
# side by side for exactly that comparison.
#
# The heap half is why these caps carry visible slack rather than sitting one
# observation above the measurement: a slab refill happens on whichever
# allocation first empties a class, so the peak moves with test order.
#
# `process` at 113 against ~109 live account rows is not a leak: the userland
# phase spawns a test binary per utest and most are still referenced at the
# dump point.
#
# 2026-08-24, one more utest binary (`dns_resolve_test`):
#   process    120 -> 121. Deterministic at 121; the cap is exact.
#   pages      655360 -> 735232, kernelmeta 6500 -> 6933. Unlike `process`
#     these move with test order, because which address spaces are still
#     unreaped at the dump point varies. Re-measured over six runs: pages
#     634 502-685 027 (spread 50 525), kernelmeta 6249-6591 (spread 342).
#     Each cap is that maximum plus the spread, so ordinary interleaving
#     cannot trip the gate while a real regression still does. The charge is released at the reap (`process_retire`), which is
# when the registry slot is given up, rather than at the final `Drop`, which
# lags for as long as any designator still resolves.
#
# 2026-09-03, one more utest binary (`persist_test`):
#   process    121 -> 122. Deterministic at 122; the cap stays exact. No other
#     row moved above its cap — a fresh --emit-allowlist over this run measured
#     pages 638 507, kernelmeta 6056 and task 5, all inside the spreads above,
#     so only the deterministic row is re-recorded and the measured margins on
#     the order-dependent rows are left alone.
#
# 2026-09-06, `persist_test` spawns one unprivileged child (the disk-reserve
# filler: the utest itself carries TASK_FLAG_SYSTEM, which is exactly the
# entitlement the reserve waives, so the filler must be a separate process):
#   process    122 -> 123. Deterministic at 123; the cap stays exact. A fresh
#     --emit-allowlist over this run measured pages 656 963, kernelmeta 2918
#     and task 5, all inside the spreads above, so as before only the
#     deterministic row moves.
#
# 2026-09-06, Phase 6 lands one more utest binary (`mount_test`, for
# `mount(2)`/`umount2(2)`):
#   process    123 -> 124. Deterministic at 124; the cap stays exact. A fresh
#     --emit-allowlist over this run measured pages 667 963, kernelmeta 3160
#     and task 5, all inside the spreads above, so as before only the
#     deterministic row moves.
#
# 2026-09-07, Phase 7 lands a ninth kind (`diskblocks`, the per-principal
# ext2 block ceiling) and five kernel tests that each spawn a scratch
# process:
#   diskblocks new rows. Both deterministic over three runs: 6 in the kernel
#     phase and 3875 in the userland phase, the latter being `persist_test`'s
#     disk-reserve filler taking the volume down to `s_r_blocks_count`. No
#     denial: the enforced per-process default (8192) sits above what that
#     filler legitimately holds, which is the whole point of `max-denials 0`.
#   kernelmeta post-kernel-tests 2200 -> 2300. The new tests' scratch
#     processes are task stacks at 12 pages each. Measured 2191/2233/2245 over
#     three runs, so the cap is the maximum plus that 54-page spread, in the
#     same style as the order-dependent rows above.
#
# `task` moves with scheduling (measured 5-7 across runs), so its cap carries
# the observed spread as margin rather than a single observation.
#
# max-denials 0 is the load-bearing line under quota=enforce: a denial means a
# legitimate workload was refused, which is a finding rather than a pass.
#
# 2026-09-09, Phase 1 of `plans/self-hosting.md` (a toolchain-sized process):
#   `kernelmeta` rises because **page tables are now charged**. Every frame
#     `VmSpace::new`, the kernel-half prepopulate and the page-table walker
#     take from the buddy is charged to the root, and with lazy heaps, lazy
#     file mappings and an 8 MiB stack extent a process's table footprint stops
#     being negligible. boot 900 -> 1787 (measured 1637-1712 over three fresh
#     runs, spread 44), post-kernel-tests 2300 -> 3172 (3106-3139, spread 33).
#     post-userland-tests measured 4949-4972, inside its existing 6933, so that
#     row is left alone.
#   `pages` rises because `brk` and `mmap` are lazy and the per-process `Pages`
#     ceiling moved to 4 GiB of VA: the new `bigprog_test` maps a gigabyte of
#     anonymous space and touches sixteen pages of it, which is the whole point
#     of the row being virtual. post-userland-tests 735232 -> 973334 (measured
#     927408-950371 over three fresh runs, spread 22963). post-kernel-tests
#     measured 43644-56309, inside its existing 65536.
#   `process` 124 -> 126: one more utest binary (`bigprog_test`) and the child
#     it forks. Deterministic at 126 over three runs; the cap stays exact.
#   `diskblocks` 3875 -> 3936: the tests image is 64M rather than 32M, so
#     `persist_test`'s space filler reaches the volume's reserve later.
#     Deterministic at 3936 over three runs. The image is deliberately no
#     larger than that: at 128M the filler met the 8192-block per-process
#     `DiskBlocks` quota instead of the reserve, which is a different test.
#   The tests guest is 1G rather than 512M (`scripts/qemu_run.sh`), because
#     `bigprog_test` holds ~170 MiB resident to cross the threshold `fork`'s
#     chunked snapshot exists for. That is why `residentpages` and `pages` are
#     both measured higher than a 512M run would report.
#   `residentpages` is new: the address space's count of present user leaves,
#     maintained by the page-table cursor and reflected into the ledger on the
#     way out of a hold of the per-process lock. It is the number `pages` never
#     was — post-userland-tests measures 64603-64640 resident against 917533-
#     950371 mapped, a factor of about eight apart, which is demand paging
#     working rather than a discrepancy. The cap is 114452 (that maximum plus
#     the spread of 1520); post-kernel-tests is 516, deterministic over the same
#     three runs, so that cap is exact. Unlimited by default: it reports what
#     is held, and the ceiling on physical memory is the frame allocator.
#     boot charges none of it (no user address space exists yet), so the
#     `min-kinds-for boot 1` floor still describes that phase.
#
# 2026-09-13, Phase 1 of `plans/self-hosting.md` (the POSIX floor a build
# system stands on):
#   `process` 126 -> 134: one more utest binary (`buildctl_test`), the phase's
#     exit-criterion build driver, which spawns a stub compiler per source
#     file plus two children it kills by signal. Deterministic at 134 over
#     three fresh runs; the cap stays exact.
#   `pages` 973334 -> 1011657 (measured 997677-1004667 over the same three
#     runs, spread 6990; the cap is that maximum plus the spread).
#     `buildctl_test` is a `std` binary and each child is another address
#     space with a lazy heap and an 8 MiB stack extent, and this row is
#     virtual, so a child that touches a handful of pages still charges its
#     whole mapping.
#   post-kernel-tests `kernelmeta` 3172 -> 3328 (measured 3152-3240, spread
#     88). The kernel phase gained 63 tests, and signal delivery now pushes a
#     3216-byte frame — restorer, `SignalFrame`, `UserSiginfo`, `UserUcontext`
#     and the XSAVE image — whose range is faulted in before the pushes, so a
#     delivering task's page tables are charged where they were not before.
#     post-userland-tests measured 4946-5054, inside its existing 6933, so
#     that row is left alone.
#   Nothing else moved above its cap and nothing else is re-recorded:
#     `residentpages` measured 110944-112929 inside 114452, `diskblocks` 3927
#     inside 3936, post-kernel-tests `pages` 53365-55170 inside 65536, and
#     `task` 5 inside its caps.
#
# 2026-09-13, the review and security sweep of that same Phase 1 change:
#   post-kernel-tests `residentpages` 516 -> 772, deterministic at 772 over
#     three fresh runs so the cap stays exact. Two causes, both new kernel
#     tests that hold user pages the kernel phase previously never populated:
#     the signal-delivery cases now fault in the 3216-byte frame range before
#     the pushes (a fault that used to livelock on a COW stack page and now
#     resolves), and the advisory-lock cases open real descriptors against the
#     ext2 root. Every other row measured inside its cap: post-kernel
#     `kernelmeta` 3141-3151 inside 3328, `pages` 51152-60223 inside 65536,
#     `process` 14 inside 20; post-userland `residentpages` 111129-112929
#     inside 114452, `kernelmeta` 4841-4967 inside 6933, `pages`
#     988042-1001043 inside 1011657, `process` 134 exact.
#
# 2026-09-14, the storage work ("Storage holds a tree" in
# plans/self-hosting.md):
#   post-kernel-tests `kernelmeta` 3328 -> 5680 (measured 5584/5606/5632 over
#     three fresh runs, spread 48). post-userland-tests 6933 -> 7642
#     (7494/7568/7568, spread 74). One cause, and it is the change working:
#     an ext2 instance's block cache is now sized from the volume it mounted
#     and there can be four instances at once. Each cache is a `KVec` of
#     descriptors plus one buddy frame per entry, charged as kernel metadata,
#     and the suite now mounts three (the boot root, the write-cost report's
#     own mount of the same image, and the journal-headroom fixture) where it
#     used to mount one. At the floor of 512 entries that is 512 frames per
#     instance — the arithmetic, not a leak: the caps moved by ~2350 and
#     ~700 pages respectively, against 512 per extra concurrent mount plus
#     the log's per-slot arrays for a 16 383-slot log.
#   Everything else stayed inside its existing cap on all three runs:
#     post-kernel-tests `residentpages` 772 exact, `pages` 53k-55k inside
#     65536, `diskblocks` 6, `process` 20; post-userland-tests
#     `residentpages` inside 114452 and `process` 134 exact.
#   Re-measured after the review fixes that followed: post-kernel-tests
#     5680 -> 6065 (5953/6009/5999 over three fresh runs, spread 56),
#     post-userland-tests 7642 -> 8128 (7780/7954/7905, spread 174). The
#     delta is the tests the review added, not the change under review: the
#     rollback/eviction case builds a 512-entry cache of its own and fills it,
#     and the read-only-mount, block-pointer and umount cases each register a
#     scratch process whose task stacks are 12 pages apiece.
#   post-userland-tests `pages` 1011657 -> 1072000: measured
#     986348/988238/997933/1029200 over four fresh runs, spread 42852. This
#     row is mapped **virtual** address space, so its spread is the widest in
#     the file and always has been — a lazily mapped `mmap` charges the whole
#     extent whether or not a page of it is ever populated, and which utests
#     overlap decides how many extents are live at the dump. The cap is the
#     maximum plus that spread. `residentpages`, the row that tracks what is
#     actually held, stayed inside 114452 on all four.

# The peak of a kind nobody exercised is zero, and a cap derived from
# it would bound nothing. Every listed kind must have been reached.
min-kinds 2

# Boot charges only the idle tasks' stacks, so it reports one kind and that
# is the honest floor for it rather than evidence of a broken report.
min-kinds-for boot 1

# A denial is an over-limit charge. Under quota=warn it is granted and
# counted, so a non-zero total means the enforced tier would have
# refused something — which is a finding, not a pass.
max-denials 0

require-phase boot
require-phase post-kernel-tests
require-phase post-userland-tests

boot	kernelmeta	1756
post-kernel-tests	custody	8
post-kernel-tests	diskblocks	6
post-kernel-tests	fdslot	256
post-kernel-tests	kernelmeta	6065
post-kernel-tests	objectrow	257
post-kernel-tests	pages	65536
post-kernel-tests	process	20
post-kernel-tests	residentpages	772
post-kernel-tests	task	8
post-userland-tests	custody	8
post-userland-tests	diskblocks	3936
post-userland-tests	fdslot	256
post-userland-tests	kernelmeta	8128
post-userland-tests	objectrow	257
post-userland-tests	pages	1072000
post-userland-tests	pinnedbytes	8
post-userland-tests	process	134
post-userland-tests	residentpages	114452
post-userland-tests	task	10

# The charge path's cost. It runs on every syscall that allocates, so a
# regression here is a whole-kernel throughput regression -- Escort measured
# end-to-end accounting at 8 %, and 15-50 % under load with protection domains.
# Without a check here a change of that size passes every other check in this
# file.
#
# Expressed as ratios, never as cycle counts. A cycle count on this path
# measures the *accelerator*: the caps this file used to carry (3000 and 9000)
# were taken on a KVM host, and the identical unmodified tree reports 20631 and
# 81886 under TCG. That gate failed on every machine without /dev/kvm, which is
# a gate measuring the host and calling it the kernel -- the same defect the
# suite's own wall-clock assertions had.
#
# One cap and two floors. All ratio values are x100, so 420 reads as 4.20.
#
#   max-depth-cost-ratio <depth> <cap>          -- the check
#       Depth 7 against depth 1. Each level is meant to add a bounded constant,
#       so this stays near 4 and a walk that stopped amortising goes to 7 or
#       beyond. It is the only quantity here that is invariant under a change
#       of accelerator, which is why it is the only one carrying a ceiling.
#
#   min-charge-over-reference <depth> <floor>   -- a measurement floor
#       One charge+refund round trip against one bare uncontended CAS round
#       trip, measured in the same run, the same batch shape and the same
#       interrupts-off window. This is deliberately a FLOOR and not a cap: the
#       ratio itself moves with the accelerator (a CAS is relatively dearer
#       under TCG than natively, so the same tree reads ~18x here and would read
#       far more under KVM), so a ceiling measured on one accelerator would fail
#       on the other -- the exact defect this file's history is about. What a
#       floor can do is prove the charge path still performs the atomic work it
#       is supposed to.
#
#   min-reference-cycles <floor>                -- a measurement floor
#       An absolute floor on the reference itself, because the two floors above
#       are ratios over it and a reference that collapsed to 1 satisfies both.
#       Not a measurement: a `lock cmpxchg` round trip through memory is not
#       under a couple of dozen cycles on any real or emulated x86, so this is
#       a physical bound and stays valid whatever runs the guest.
#
# **What is NOT checked, stated plainly:** a slowdown that scales the whole
# charge path uniformly -- every depth, and a bare CAS with it -- passes every
# line above. Catching that needs an absolute cycle ceiling, and an absolute
# cycle ceiling is what this file used to carry and what failed on every machine
# without /dev/kvm. Restoring one costs a measurement on each accelerator CI
# might use, which nobody has; a gate nobody can run green is worse than a gate
# with a stated hole.

# The tightest quantity in this file: 3.77 and 3.86 in the two pinned runs that
# reached the report, 3.69-3.99 across the earlier runs. The cap is 5.00; a walk
# that stopped amortising goes to 7.00 (one full charge per level) or beyond.
max-depth-cost-ratio 7 500

# Floors, emitted at half the TCG observation. TCG is the conservative source
# for these: it makes an atomic relatively dearer than the surrounding work, so
# charge-over-CAS is *lower* here (18.5x at depth 1) than it would be natively.
min-charge-over-reference 1 900
min-charge-over-reference 7 3400

# The denominator those two divide by. A physical bound, not a measurement.
min-reference-cycles 20
