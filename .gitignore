.idea/
.cache/
builddir/
third_party/*
# The owned `slopos` sysroot (scripts/make_slopos_sysroot.sh materialises it
# from the pinned toolchain plus the toolchain/ fork overlay). Named rather
# than left to `third_party/*` above because it is generated, not fetched.
third_party/rust-slopos/
# The pinned rustc source tree carrying the compiler fork
# (scripts/make_rustc_src.sh), named for the same reason.
third_party/slopos-rustc-src/
EFI/
boot_fat/
test_efi_dir/
test_output.log
boot_test.log
*.swp
slop.iso
*.iso
iso/boot/kernel.elf
iso/EFI/BOOT/BOOTX64.EFI

# Local knowledge index artifacts
knowledge/.venv/
knowledge/.cache/
knowledge/index.sqlite
knowledge/index.sqlite-shm
knowledge/index.sqlite-wal
knowledge/index.sqlite-journal

# Bun artifacts for MCP tooling
mcp/.bun/
mcp/.turbo/
mcp/.cache/
mcp/*.tsbuildinfo

# Rust build artifacts
target/
rust/target/
rust/**/target/
**/*.rs.bk

# Runtime filesystem images (generated by build/test)
fs/assets/ext2.img
fs/assets/ext2-tests.img
fs/assets/ext2-persist.img
fs/assets/ext2-capacity.img
fs/assets/ext2-devdisk.img
fs/assets/*.img.stamp

# Editor/IDE config
.vscode/

# Sisyphus agent artifacts
.sisyphus/

# Python cache artifacts
__pycache__/
*.pyc
